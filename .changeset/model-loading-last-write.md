---
'@rabjs/service': patch
---

Document that `$model.method.loading` is a boolean last-write flag (not a pending count) and `$model.method.error` cannot be attributed under overlapping same-method calls.
